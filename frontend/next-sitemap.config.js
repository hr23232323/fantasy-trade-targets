module.exports = {
  siteUrl: "https://www.fantasytradetarget.com",
  generateRobotsTxt: false,
  // Deployment time is not page freshness. Add dataset-aware lastmod values
  // through a transform once public releases carry per-page timestamps.
  autoLastmod: false,
};
